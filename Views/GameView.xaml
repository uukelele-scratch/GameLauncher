<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GameLauncher.Views.GameView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameLauncher.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:GameLauncher.ViewModels"
    mc:Ignorable="d">

    <Grid>
        <TextBlock x:Name="messageTextBlock" Text="" FontSize="16" Margin="20,20,20,0" HorizontalAlignment="Center" TextAlignment="Center" VerticalAlignment="Top" Visibility="Collapsed"/>
        <GridView x:Name="gamesGrid" ItemsSource="{x:Bind Games, Mode=OneWay}" IsItemClickEnabled="True" ItemClick="gamesGrid_ItemClick" SelectionMode="None" Padding="20,10,20,20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:GameViewModel">
                    <Grid Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="5" Padding="10" Width="170" Height="270" Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border CornerRadius="5">
                            <Image Grid.Row="0" Source="{x:Bind ImagePath}" VerticalAlignment="Top"/>
                        </Border>
                        <TextBlock Grid.Row="1" Text="{x:Bind Name}" FontWeight="SemiBold" Margin="0,8,0,2" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{x:Bind Name}" TextAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>
        <ProgressRing x:Name="loadingIndicator" IsActive="False"
                      HorizontalAlignment="Center" VerticalAlignment="Center"
                      Width="50" Height="50"/>
    </Grid>
</Page>
